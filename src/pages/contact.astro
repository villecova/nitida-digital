---
import Footer from '../components/Footer.astro';
import Navbar from '../components/Navbar.astro';
import Welcome from '../components/Welcome.astro';
import ContactForm from '../components/shared/ContactForm.svelte';
import { site } from '../data/site';
import Layout from '../layouts/Layout.astro';

const pageTitle = 'Contacto — Cuéntame sobre tu proyecto | Nítida Digital';
const pageDescription =
  'Cuéntame qué necesitas: sitio web, identidad visual o ambos. Respuesta en menos de 24 horas hábiles.';
const pageUrl = `${site.siteUrl}/contact`;
const imageUrl = `${site.siteUrl}/assets/images/og_image.png`;
---

<Layout pageTitle={pageTitle} pageDescription={pageDescription} pageUrl={pageUrl} imageUrl={imageUrl}>
  <Navbar currentPath="/contact" />

  <Welcome
    type="color"
    color="bg-gradient-to-r from-gray-900 via-black to-gray-800"
    title="Hablemos sobre tu proyecto"
    subtitle="Cuéntame qué necesitas. Revisamos alcance, tiempos y presupuesto juntos, sin compromiso."
    primaryButton="Ir al formulario"
    primaryLink="#formulario"
    secondaryButton=""
    secondaryLink=""
    mediaVariant="logo"
  />

  <section class="py-16 md:py-24 bg-white" id="contacto">
    <div class="max-w-6xl mx-auto px-4">
      <div class="grid lg:grid-cols-5 gap-12">
        <div class="lg:col-span-3" data-aos="fade-up" data-aos-delay="100">
          <h2 class="text-3xl md:text-4xl font-main text-gray-900 mb-3">Formulario de contacto</h2>
          <p class="text-accent mb-8 text-base">
            Completa los campos. Con esa información puedo darte una orientación clara sobre el siguiente paso.
          </p>
          <ContactForm
            client:load
            formspreeEndpoint={site.formspreeEndpoint}
            emailFallback={site.email}
          />
        </div>

        <aside class="lg:col-span-2 space-y-6" data-aos="fade-up" data-aos-delay="150">
          <div class="bg-slate-50 rounded-2xl p-6 border border-gray-200">
            <h3 class="text-lg font-bold text-gray-900 mb-2">Correo</h3>
            <p class="text-sm text-gray-600 mb-4">
              Para consultas detalladas o si prefieres escribir directo.
            </p>
            <a
              href={site.mailtoWithSubject(
                'Consulta desde web',
                'Hola, me gustaría conocer más sobre tus servicios. Gracias.'
              )}
              class="block w-full bg-gray-900 text-white font-semibold py-3 px-6 rounded-xl text-center hover:bg-black transition-colors text-sm"
              data-track="email_click"
            >
              {site.email}
            </a>
          </div>

          {site.hasWhatsApp && site.whatsappUrl && (
            <div class="bg-slate-50 rounded-2xl p-6 border border-gray-200">
              <h3 class="text-lg font-bold text-gray-900 mb-2">WhatsApp</h3>
              <p class="text-sm text-gray-600 mb-4">Respuesta rápida por mensaje.</p>
              <a
                href={site.whatsappUrl}
                target="_blank"
                rel="noopener noreferrer"
                class="block w-full bg-green-600 text-white font-semibold py-3 px-6 rounded-xl text-center hover:bg-green-700 transition-colors text-sm"
                data-track="whatsapp_click"
              >
                Enviar mensaje
              </a>
            </div>
          )}

          <div class="bg-white rounded-2xl p-6 border border-gray-200">
            <h3 class="text-lg font-bold text-gray-900 mb-4">Qué pasa después</h3>
            <ul class="space-y-3 text-sm text-gray-600">
              <li class="flex gap-2">
                <span class="text-gray-900 font-semibold">1.</span>
                Leo tu mensaje y contesto en menos de 24 horas hábiles.
              </li>
              <li class="flex gap-2">
                <span class="text-gray-900 font-semibold">2.</span>
                Si encaja, agendamos una llamada breve para alinear objetivos.
              </li>
              <li class="flex gap-2">
                <span class="text-gray-900 font-semibold">3.</span>
                Te envío una propuesta de alcance y cotización.
              </li>
            </ul>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<script>
  import { track } from '../lib/analytics';

  document.querySelectorAll('[data-track]').forEach((el) => {
    el.addEventListener('click', () => {
      const event = el.getAttribute('data-track');
      if (event) track(event, { source: 'contact' });
    });
  });
</script>
